server:
  port: 8080
  servlet:
    context-path: /cms
spring:
  application:
    name: SmashHub
  datasource:
    url: "jdbc:mysql://localhost:3306/badminton_shop"
    username: root
    password: root
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true